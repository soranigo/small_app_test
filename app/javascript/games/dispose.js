const btn = document.getElementByIClassName('dispose');
const score_objects = document.getElementById('dispose').value1;
const score_object = a; //score_objectsの最初のレコード
const score = document.getElementById('dispose').value2;

btn.addEventListener("click", () => {
  //取得したscore_objectのscoreカラムの数値分を減点し、score_objectの消去
});